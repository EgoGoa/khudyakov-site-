{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "khudyakov-site-start",
      "runtimeExecutable": "sh",
      "runtimeArgs": [
        "-c",
        "if lsof -nP -iTCP:3000 -sTCP:LISTEN >/dev/null 2>&1; then echo 'Reusing the next dev server already running on :3000 (another session started it)'; exec tail -f /dev/null; else rm -f .next/dev/lock; exec npm run dev -- -p 3000; fi"
      ],
      "port": 3000
    }
  ]
}
