// Типографские константы, общие для всего сайта.
//
// Живут в обычном модуле, а не внутри CinematicSection: тот помечен
// "use client", и серверный компонент, импортировавший оттуда константу,
// получал не строку, а клиентскую ссылку — класс подставлялся как текст
// функции и стиль молча не применялся. Значение одно на весь сайт, поэтому
// у него нет причин быть привязанным к клиентскому компоненту.

/** Подзаголовок главы — строка под заголовком. Белая и в читаемом кегле, а
 *  не мелкий капс-caption: Егор просил вернуть этой строке тот вес, который
 *  она несёт на /sites, и сделать так на каждой странице. */
export const CHAPTER_INTRO =
  "font-display text-sm uppercase leading-snug tracking-tight text-white sm:text-base";

/** Лид первого экрана — строка под заголовком героя.
 *
 *  Тот же приём, что у CHAPTER_INTRO, только на кегль крупнее: это первая
 *  фраза страницы, и она должна держать вес заголовка над собой. На
 *  страницах направлений здесь стоял обычный строчный текст, и Егор
 *  показал скриншотом, что он выпадает из языка сайта — «сделай как на
 *  основных страницах, крупнее, стильнее и толще». Дисплейный шрифт
 *  (Bebas) узкий, поэтому в тот же блок помещается заметно больший кегль. */
export const HERO_LEAD =
  "font-display text-lg uppercase leading-snug tracking-tight text-white sm:text-2xl lg:text-[1.75rem]";

/** Надзаголовок блока — короткая строка капсом над заголовком («НАШИ
 *  РАБОТЫ», «ЗАДАЧА В ЦЕНТРЕ ВНИМАНИЯ», «ПЕРСОНАЛИЗАЦИЯ»).
 *
 *  Набирается основным шрифтом сайта, а не моноширинным. Причин две.
 *
 *  Первая — техническая: моноширинный здесь долго стоял без кириллицы, и
 *  русские буквы подменялись системным шрифтом, а цифры оставались своими.
 *  Строка «ШАГ 1 ИЗ 3» собиралась из двух гарнитур и заметно скакала по
 *  высоте. Сам шрифт мы починили (см. layout.tsx), но надзаголовку моно и
 *  не нужен.
 *
 *  Вторая — та, ради которой Егор и просил переделать: моноширинный давал
 *  строке «консольный», айтишный характер, а страница должна читаться
 *  презентационно. Полужирный Manrope с широким трекингом звучит спокойнее
 *  и дороже, и при этом остаётся такой же тихой служебной строкой.
 *
 *  Цвет не задан намеренно: у надзаголовков он разный по страницам
 *  (text-rec на направлениях, text-glow на /sites и /smm). */
export const EYEBROW =
  "font-sans text-[11px] font-bold uppercase tracking-[0.24em]";
